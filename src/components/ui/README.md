# UI Components

This folder contains reusable, atomic UI primitives managed by shadcn/ui.

To add components to this folder, run:

```bash
npx shadcn@latest add [component-name]
```

Example:

```bash
npx shadcn@latest add button
```
